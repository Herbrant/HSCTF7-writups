# Binary Word Search
You have to take the pixel value and see all the image as a matrix.
The you have to find the string 'flag' in the matrix (binary search).
See search.py file the execute it:

```
python search.py
```